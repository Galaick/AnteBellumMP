country_decisions = {
	
	rename_angevin_empire = {
		major = yes
		potential = {
			tag = DLM
			has_country_flag = angevin_rename
		}
		allow = {
			tag = DLM
			dynasty = "d'Anjou"
		}
		effect = {
			clr_country_flag = angevin_rename
			override_country_name = RENAME_ANGEVIN
		}
		ai_will_do = {
			factor = 1
		}
	}
}